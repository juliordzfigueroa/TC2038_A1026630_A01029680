#include <iostream>

using namespace std;

int main() {
    int a, b; // Variables para almacenar los números
    cin >> a >> b; // Inputs solicitados al usuario en el formato requerido a "" b
    cout << a + b << endl; // Salida del resultado de la suma
    return 0;
}